meta {
  name: change_user
  type: http
  seq: 4
}

patch {
  url: http:/localhost:3000/users/{id}?id
  body: json
  auth: inherit
}

params:query {
  id: 
}

body:json {
  {
    "email": "string",
    "firstName": "string",
    "lastName": "string",
    "pin": "string",
    "password": "string",
    "role": "string"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
